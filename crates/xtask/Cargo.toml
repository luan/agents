[package]
name = "xtask"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
anyhow = "^1"
clap = { version = "^4", features = ["derive"] }
dirs = "^6"
regex = "^1"
serde = { version = "^1", features = ["derive"] }
serde_json = "^1"
tempfile = "^3"
toml = "^0.8"
walkdir = "^2"

